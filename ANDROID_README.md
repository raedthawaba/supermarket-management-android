# تطبيق إدارة السوبر ماركت للأندرويد

تطوير: **MiniMax Agent**  
تاريخ: 2025-11-04

## نظرة عامة

تطبيق Android لإدارة السوبر ماركت مبني باستخدام Python و Kivy، يوفر واجهة مستخدم محسنة للهواتف مع جميع ميزات إدارة المتجر.

## الميزات الرئيسية

### 🛍️ إدارة المنتجات
- عرض جميع المنتجات مع صور وأوصاف
- البحث في المنتجات
- فلترة حسب الفئات (مشروبات، أطعمة، خضروات، إلخ)
- عرض تفاصيل المنتج والسعر والمخزون

### 🛒 سلة التسوق
- إضافة المنتجات للسلة
- تعديل الكميات
- حساب المجموع التلقائي
- إفراغ السلة

### 💰 إتمام الشراء
- إنشاء فواتير جديدة
- اختيار طريقة الدفع (نقدي/بطاقة)
- طباعة تفاصيل الفاتورة
- حفظ في قاعدة البيانات

### 📱 واجهة محسنة للهواتف
- تصميم responsive
- دعم اللغة العربية
- ألوان وأيقونات جذابة
- سهولة في الاستخدام

## المتطلبات

### للتطوير:
- Python 3.8+
- Kivy 2.3.1
- KivyMD 1.2.0
- Buildozer 1.5.0

### للتشغيل:
- Android 5.0 (API 21) أو أحدث
- ذاكرة تخزين 50 ميجابايت على الأقل

## التثبيت والتشغيل

### 1. تحضير البيئة
```bash
# تثبيت المتطلبات
pip install -r requirements_android.txt

# أو تثبيت يدوياً
pip install kivy==2.3.1 kivymd==1.2.0 buildozer==1.5.0
```

### 2. تشغيل التطبيق محلياً (للتطوير)
```bash
python main.py
```

### 3. بناء ملف APK

#### الطريقة الأولى: استخدام Buildozer (الأسهل)
```bash
# بناء APK
buildozer android debug

# أو للإصدار الموقعة للإنتاج
buildozer android release
```

#### الطريقة الثانية: البناء اليدوي
```bash
# الدخول لمجلد المشروع
cd android_app

# إعداد بيئة Android
buildozer init

# بناء APK
buildozer android debug
```

### 4. تثبيت APK على الجهاز
```bash
# نسخ الملف للجهاز
adb install bin/SupermarketManager-1.0-arm64-v8a-debug.apk

# أو نسخ الملف يدوياً وتثبيته
```

## بنية المشروع

```
📦 تطبيق السوبر ماركت
├── 📄 main.py                    # نقطة الدخول
├── 📄 main_android_app.py       # واجهة المستخدم الرئيسية
├── 📄 android_supermarket_app.py # منطق الأعمال وقاعدة البيانات
├── 📄 buildozer.spec            # إعدادات البناء
├── 📄 requirements_android.txt  # المتطلبات
└── 📄 README.md                 # هذه الوثيقة
```

## قاعدة البيانات

التطبيق يستخدم SQLite مع الجداول التالية:

### جدول المنتجات (products)
- `id`: معرف المنتج
- `name`: اسم المنتج
- `price`: السعر
- `category`: الفئة
- `stock`: المخزون
- `created_at`: تاريخ الإنشاء
- `updated_at`: تاريخ التحديث

### جدول الفواتير (invoices)
- `id`: معرف الفاتورة
- `invoice_number`: رقم الفاتورة
- `customer_name`: اسم العميل
- `total_amount`: إجمالي المبلغ
- `payment_method`: طريقة الدفع
- `created_at`: تاريخ الإنشاء

### جدول تفاصيل الفواتير (invoice_details)
- `id`: معرف التفاصيل
- `invoice_id`: معرف الفاتورة
- `product_id`: معرف المنتج
- `product_name`: اسم المنتج
- `quantity`: الكمية
- `unit_price`: السعر للوحدة
- `total_price`: السعر الإجمالي

## الميزات المضافة في النسخة الجديدة

### 🔐 الأمان
- حماية من Path Traversal
- التحقق من صحة المدخلات
- تشفير كلمات المرور
- نظام تسجيل الأحداث

### 📱 واجهة المستخدم
- تصميم Material Design
- دعم اللمس والإيماءات
- ألوان متناسقة
- أيقونات واضحة

### ⚡ الأداء
- تحميل سريع للقائمة
- استعلامات محسنة
- ذاكرة مؤقتة ذكية
- استجابة سريعة

### 🌍 دعم اللغة
- واجهة عربية كاملة
- اتجاه من اليمين لليسار
- خطوط واضحة
- نصوص مفهومة

## الشاشة الرئيسية

يحتوي التطبيق على ثلاث شاشات رئيسية:

### 1. شاشة المنتجات
- شريط بحث
- فلاتر الفئات
- قائمة المنتجات
- زر الإضافة للسلة

### 2. شاشة السلة
- قائمة المنتجات المختارة
- تعديل الكميات
- حساب المجموع
- إتمام الشراء

### 3. شاشة الفواتير
- عرض الفواتير السابقة
- تفاصيل كل فاتورة
- إحصائيات المبيعات

## إعدادات البناء المخصصة

في ملف `buildozer.spec`:

```ini
# معلومات التطبيق
title = نظام إدارة السوبر ماركت
package.name = supermarketmanager
package.domain = com.minimax
version = 1.0

# المتطلبات
requirements = python3,kivy,kivymd,sqlite3

# الإعدادات
orientation = portrait
fullscreen = 0

# الأذونات
android.permissions = INTERNET,WRITE_EXTERNAL_STORAGE,READ_EXTERNAL_STORAGE
```

## اختبار التطبيق

### محلياً:
```bash
# تشغيل التطبيق للتأكد من عمله
python main.py

# اختبار قاعدة البيانات
python -c "from android_supermarket_app import DatabaseManager; db = DatabaseManager(); print('✅ قاعدة البيانات تعمل')"
```

### على Android:
```bash
# تشغيل اختبارات الأداء
adb shell am start -n com.minimax.supermarket/.SupermarketApp

# عرض السجلات
adb logcat | grep Supermarket
```

## استكشاف الأخطاء

### مشاكل شائعة:

#### 1. خطأ في البناء
```bash
# تحديث buildozer
pip install --upgrade buildozer

# تنظيف الملفات المؤقتة
buildozer clean
```

#### 2. خطأ في Kivy
```bash
# تحديث Kivy
pip install --upgrade kivy kivymd

# التأكد من إصدار Python
python --version
```

#### 3. مشاكل الذاكرة
```bash
# زيادة heap size في buildozer.spec
android.add_compile_options = "sourceCompatibility = 1.8", "targetCompatibility = 1.8"
```

## التطوير المستقبلي

### ميزات مخططة:
- 🔄 مزامنة البيانات مع السحابة
- 📊 تقارير المبيعات المتقدمة
- 👥 إدارة المستخدمين والصلاحيات
- 🏷️ طباعة الباركود
- 📲 دعم الإشعارات
- 🌐 دعم لغات إضافية

### تحسينات:
- تحسين الأداء
- إضافة اختبارات آلية
- دعم التحديث التلقائي
- تحسين الأمان

## الدعم والمساهمة

لتبليغ الأخطاء أو طلب ميزات جديدة:
- 📧 البريد الإلكتروني: [البريد الإلكتروني]
- 🐛 GitHub Issues: [رابط المستودع]
- 💬 المنتدى: [رابط المنتدى]

## الترخيص

هذا المشروع مرخص تحت رخصة MIT - انظر ملف LICENSE للتفاصيل.

## شكر وتقدير

- فريق Kivy على إطار العمل الرائع
- فريق KivyMD على المكونات الجذابة
- مجتمع Python على الدعم والمساعدة

---

**تم التطوير بواسطة MiniMax Agent** ❤️  
*نسخة 1.0 - نوفمبر 2025*